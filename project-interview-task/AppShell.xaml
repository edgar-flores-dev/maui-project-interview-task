<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="project_interview_task.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:project_interview_task.Views"
    xmlns:fontAwesome="clr-namespace:FontAwesome"
    Shell.FlyoutBehavior="Disabled"
    Title="project_interview_task">

    <Shell.Resources>
        <ResourceDictionary>
            <Style x:Key="BaseStyle" TargetType="Element">
                <Setter Property="Shell.BackgroundColor" Value="{StaticResource Primary}" />
                <Setter Property="Shell.ForegroundColor" Value="White" />
                <Setter Property="Shell.TitleColor" Value="White" />
                <Setter Property="Shell.DisabledColor" Value="#B4FFFFFF" />
                <Setter Property="Shell.UnselectedColor" Value="#95FFFFFF" />
                <Setter Property="Shell.TabBarBackgroundColor" Value="{StaticResource Primary}" />
                <Setter Property="Shell.TabBarForegroundColor" Value="White"/>
                <Setter Property="Shell.TabBarUnselectedColor" Value="#95FFFFFF"/>
                <Setter Property="Shell.TabBarTitleColor" Value="White"/>
            </Style>
            <Style TargetType="TabBar" BasedOn="{StaticResource BaseStyle}" />
            <Style TargetType="FlyoutItem" BasedOn="{StaticResource BaseStyle}" />
        </ResourceDictionary>
    </Shell.Resources>

    <TabBar>
        <ShellContent Title="Rotate" Route="RotateView" ContentTemplate="{DataTemplate local:RotateView}" >
            <ShellContent.Icon>
                <FontImageSource FontFamily="FAS" Glyph="{x:Static fontAwesome:FontAwesomeIcons.Sync}"></FontImageSource>
            </ShellContent.Icon>
        </ShellContent>
        <ShellContent Title="Info" Route="UserDeviceInfoView" ContentTemplate="{DataTemplate local:UserDeviceInfoView}">
            <ShellContent.Icon>
                <FontImageSource FontFamily="FAS" Glyph="{x:Static fontAwesome:FontAwesomeIcons.InfoCircle}"></FontImageSource>
            </ShellContent.Icon>
        </ShellContent>
    </TabBar>

</Shell>
